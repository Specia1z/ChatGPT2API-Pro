# ───── 敏感配置（含密钥，绝不提交）─────
.env
.env.local
.env.prod
.env*.local
server/.env
web/.env.local

# ───── 编译产物 / 二进制 ─────
server.exe
server.exe~
*.exe
*.exe~
/server/server
/server/reset_admin

# ───── 前端依赖与构建 ─────
node_modules/
.next/
out/
build/
dist/
*.tsbuildinfo
next-env.d.ts

# ───── 本地上传文件（用户图片，不入库）─────
server/uploads/
uploads/

# ───── Go ─────
*.test
*.out

# ───── 工具 / 编辑器 / 系统 ─────
.claude/
.vscode/
.idea/
.DS_Store
Thumbs.db
*.log
npm-debug.log*

# ───── 根目录开发垃圾（旧副本 / 一次性脚本，不入库）─────
/mysql.go
/fix-sizes.js
/test_email.js
/deploy-src.tar.gz
/backend-src.tar.gz

# ───── 本地环境特定测试（硬编码本地 DB/Redis，不入 CI）─────
/server/internal/api/admin_login_test.go

# ───── 备份文件（开发中旧版本快照，不入库）─────
*.bak

# ───── 构建归档 / 临时包 ─────
*.tar.gz
*.zip

# ───── 跨平台编译产物 ─────
server/server_*

# ───── 根目录临时依赖（项目用 web/ 下的）─────
/package.json
/package-lock.json

# ───── 调试 / 一次性测试脚本 ─────
/test_func.*
/reasonix

# ───── 其他工具缓存 ─────
.reasonix/

# Python 缓存
__pycache__/
*.pyc
*.egg-info/
